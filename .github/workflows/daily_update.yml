name: Daily Content Update

on:
  schedule:
    # 毎日 UTC 0:00 (日本時間 9:00) に実行
    - cron: '0 0 * * *'
  push:
    branches:
      - main
    paths-ignore:
      - manus_input.md
      - used_links.json
  workflow_dispatch: # 手動でテスト実行するためのボタンを表示

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Commit and Push changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git pull --rebase origin main
          python main.py
          git add manus_input.md used_links.json
          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git commit -m "chore: update daily manus input"
          if git push origin HEAD:main; then
            exit 0
          fi

          echo "Push failed due to concurrent update; retrying once..."
          git fetch origin main
          git reset --hard origin/main
          python main.py
          git add manus_input.md used_links.json
          if git diff --cached --quiet; then
            echo "No changes to commit after refresh"
            exit 0
          fi
          git commit -m "chore: update daily manus input (retry)"
          git push origin HEAD:main
